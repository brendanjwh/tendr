<!DOCTYPE html>
<html>
  <head>
    <title>Tendr</title>
    <%= csrf_meta_tags %>
   <head>
    <!--Import Google Icon Font-->
    <link href="http://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>   

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
  <!-- ================================================== -->

<body class="container card-panel blue-grey">

<!-- top row welcome and image -->
  <div class="row blue-grey lighten-1">
    <div class="col s3 m4 l2">
      <%= link_to root_path do %>
        <%= image_tag "logo.jpg" %>
      <% end %>
    </div>
    <div class="col s9">
      <h2>Welcome to Tendr</h2>
    </div>
  </div>

<!-- side nav bar -->
  <div class="row blue-grey lighten-3">
    <div class="col s2">
      <ul class="container card-panel grey">
        <% if user_signed_in? %>
          <li><%= link_to "Logout (#{current_user.email})", destroy_user_session_path, :method => :delete %></li>
        <% else %>
          <li><%= link_to 'Login', new_user_session_path  %></li>
        <% end %>
        <% if user_signed_in? %>
          <li><%= link_to 'Create Recipe', new_recipe_path %></li>
        <% else %>
          <li><%= link_to 'Register Now', new_user_registration_path %></li>
        <% end %>
      </ul>
      <ul class="container card-panel grey">
        <li>Social</li>
        <li><a href="/about">About</a>
        <li><a href="mailto:brendanjwh@gmail.com">Email</a></li>
        <li><a href="https://www.github.com/brendanjwh">Github</a></li>
      </ul>
    </div>
  </div>

  <!-- new recipe button & link -->
  <div class="buttons">
    <button class="button"><%= link_to "New Recipe", new_recipe_path %></button>
  </div>

  <div class="col s10">
    <%= yield %>
  </div>

  <% flash.each do |name, msg| %>
    <%= content_tag(:div, msg, class: "alert") %>
  <% end %>
 

</body>
</html>
